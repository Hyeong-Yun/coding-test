function solution(tickets) {
  let visited = [tickets[0][0]];
  let queue = [tickets[0][0]];

  while (queue.length > 0) {
    let first = queue.shift();
  }

  return;
}

console.log(
  solution([
    ["ICN", "JFK"],
    ["HND", "IAD"],
    ["JFK", "HND"],
  ])
);
